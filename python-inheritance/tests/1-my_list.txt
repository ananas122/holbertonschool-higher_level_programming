=============================
The ``1-my_list`` module
=============================

Using ``MyList``
---------------------

Import the class:

    >>> MyList = __import__('1-my_list').MyList

TEST :

    >>> my_list = MyList()

    >>> my_list
    []

    >>> my_list.append(1)

    >>> my_list.append(4)

    >>> my_list.append(2)

    >>> my_list
    [1, 4, 6]

    >>> my_list.print_sorted()
    [1, 6, 4]

    >>> my_list
    [1, 4, 6]

    >>> my_list = MyList([0, 1, 3])

    >>> my_list
    [0, 1, 3]

    >>> my_list.print_sorted()
    [3, 0, 1]

    >>> my_list = MyList("Good")
    
    >>> my_list
    ['G', 'o', 'o', 'd']

    >>> my_list.print_sorted()
    ['o', 'G', 'o', 'd']

    >>> new_l = MyList()

    >>> new_l.print_sorted()
    []
